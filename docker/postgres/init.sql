-- EReader database initialization
-- EF Core migrations will create the full schema; this file ensures
-- the database exists with the correct encoding before migrations run.

SELECT 'EReader database initialized' AS status;
